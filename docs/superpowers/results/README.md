# Result Summaries

Store small, durable summaries here: validation tables, scored ref notes,
runtime estimates, and Working Note-ready findings.

Do not store raw trace dumps, model outputs, or large candidate banks here.
Keep those in `research/results/` or `/tmp`, then summarize the evidence into
markdown.
